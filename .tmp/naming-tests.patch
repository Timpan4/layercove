diff --git a/backend/tests/_fixtures/moonraker.py b/backend/tests/_fixtures/moonraker.py
index 5e3314f..472f8fd 100644
--- a/backend/tests/_fixtures/moonraker.py
+++ b/backend/tests/_fixtures/moonraker.py
@@ -25,2 +25,3 @@ class FakeMoonraker:
     uploads: list[tuple[str, bytes]] = field(default_factory=list)
+    upload_paths: list[str] = field(default_factory=list)
     malformed_server_info: bool = False
@@ -156,2 +157,3 @@ class FakeMoonraker:
         root: str | None = None
+        directory = "queue"
         filename: str | None = None
@@ -161,2 +163,4 @@ class FakeMoonraker:
                 root = await part.text()
+            elif part.name == "path":
+                directory = await part.text()
             elif part.name == "file":
@@ -168,3 +172,4 @@ class FakeMoonraker:
         self.uploads.append((filename, bytes(content)))
-        remote_path = f"queue/{filename}"
+        remote_path = f"{directory}/{filename}"
+        self.upload_paths.append(remote_path)
         return web.json_response({"item": {"root": "gcodes", "path": remote_path}}, status=201)
diff --git a/backend/tests/integration/test_catalog_klipper_pipeline.py b/backend/tests/integration/test_catalog_klipper_pipeline.py
index f1bdd92..be83b71 100644
--- a/backend/tests/integration/test_catalog_klipper_pipeline.py
+++ b/backend/tests/integration/test_catalog_klipper_pipeline.py
@@ -123,3 +123,3 @@ async def test_catalog_slice_reaches_moonraker_as_raw_gcode(
 
-    raw_gcode = b"; gcode_flavor = klipper\nG28\nG1 X10 Y10\n"
+    raw_gcode = b"; gcode_flavor = klipper\n; filament_type = PLA\nG28\nG1 X10 Y10\n"
     packaged = io.BytesIO()
@@ -140,3 +140,7 @@ async def test_catalog_slice_reaches_moonraker_as_raw_gcode(
         captured.append((str(request.url), fields))
-        return httpx.Response(200, content=packaged.getvalue() if packaged_response else raw_gcode)
+        return httpx.Response(
+            200,
+            content=packaged.getvalue() if packaged_response else raw_gcode,
+            headers={"x-print-time-seconds": "582"},
+        )
 
@@ -250,3 +254,3 @@ async def test_catalog_slice_reaches_moonraker_as_raw_gcode(
         artifact = await db_session.get(LibraryFile if source_kind == "library" else PrintArchive, artifact_id)
-        assert artifact.filename.endswith(".gcode") and not artifact.filename.endswith(".3mf")
+        assert artifact.filename == ("cube_PLA_9m42s.gcode" if source_kind == "library" else "source_PLA_9m42s.gcode")
         assert (tmp_path / artifact.file_path).read_bytes() == raw_gcode
@@ -287,6 +291,10 @@ async def test_catalog_slice_reaches_moonraker_as_raw_gcode(
         assert remote_name.endswith(".gcode")
-        assert remote_name.startswith(f"{Path(artifact.filename).stem}-")
-        assert fake_moonraker.commands == [("start", f"queue/{remote_name}")]
+        assert remote_name == artifact.filename
+        assert fake_moonraker.commands == [("start", fake_moonraker.upload_paths[0])]
         queued_row = await db_session.get(PrintQueueItem, item_id)
         assert queued_row.provider_correlation_id
+        assert (
+            fake_moonraker.upload_paths[0]
+            == f"layercove/{queued_row.provider_correlation_id.replace('-', '')}/{remote_name}"
+        )
         assert queued_row.start_reconcile_after is None
diff --git a/backend/tests/integration/test_fake_moonraker.py b/backend/tests/integration/test_fake_moonraker.py
index 92e66a8..da86ea4 100644
--- a/backend/tests/integration/test_fake_moonraker.py
+++ b/backend/tests/integration/test_fake_moonraker.py
@@ -345,3 +345,3 @@ async def test_queue_lifecycle_runs_through_fake_backed_backend(
                 assert correlation_id
-                assert remote_path == f"queue/cube-plate-2-{correlation_id.replace('-', '')}.gcode"
+                assert remote_path == f"layercove/{correlation_id.replace('-', '')}/cube.gcode"
                 assert (await db.get(PrintArchive, ids.archive)).filename == source.name
diff --git a/backend/tests/unit/test_moonraker_http.py b/backend/tests/unit/test_moonraker_http.py
index 61cb3dd..8baf097 100644
--- a/backend/tests/unit/test_moonraker_http.py
+++ b/backend/tests/unit/test_moonraker_http.py
@@ -894 +894,72 @@ async def test_start_accepts_moonraker_ok_response():
     await client.start_print("queue/cube.gcode")
+
+
+@pytest.mark.asyncio
+async def test_dispatch_directories_preserve_basename_and_separate_repeated_uploads():
+    from email.parser import BytesParser
+    from email.policy import default
+
+    from backend.app.services.moonraker_http import MoonrakerHTTPClient
+
+    async def resolver(_host, _port):
+        return {ipaddress.ip_address("192.168.1.25")}
+
+    received = []
+
+    def handler(request):
+        message = BytesParser(policy=default).parsebytes(
+            f"Content-Type: {request.headers['content-type']}\r\n\r\n".encode() + request.content
+        )
+        parts = {part.get_param("name", header="content-disposition"): part for part in message.iter_parts()}
+        directory = parts["path"].get_payload(decode=True).decode()
+        filename = parts["file"].get_filename()
+        assert filename == "3dbenchy_PLA_9m42s.gcode"
+        assert parts["root"].get_payload(decode=True) == b"gcodes"
+        assert "print" not in parts
+        received.append((directory, filename))
+        return httpx.Response(201, json={"item": {"root": "gcodes", "path": f"{directory}/{filename}"}})
+
+    client = MoonrakerHTTPClient(
+        base_url="http://printer.lan:7125", resolver=resolver, transport_factory=lambda *_: httpx.MockTransport(handler)
+    )
+    for attempt in ("a" * 32, "b" * 32):
+        directory = f"layercove/{attempt}"
+        path = await client.upload_gcode(
+            io.BytesIO(b"G28"), filename="3dbenchy_PLA_9m42s.gcode", size=3, directory=directory
+        )
+        assert path == f"{directory}/3dbenchy_PLA_9m42s.gcode"
+    assert received[0] != received[1]
+
+
+@pytest.mark.asyncio
+@pytest.mark.parametrize(
+    "directory", ["../escape", "/gcodes", "layercove//job", "layercove/./job", "layercove/job\x00"]
+)
+async def test_upload_rejects_invalid_directory_before_network(directory):
+    from backend.app.services.moonraker_http import MoonrakerHTTPClient, MoonrakerHTTPError
+
+    async def resolver(*_):
+        pytest.fail("invalid upload path reached the network")
+
+    client = MoonrakerHTTPClient(base_url="http://printer.lan:7125", resolver=resolver)
+    with pytest.raises(MoonrakerHTTPError, match="invalid_directory"):
+        await client.upload_gcode(io.BytesIO(b"G28"), filename="cube.gcode", directory=directory)
+
+
+@pytest.mark.asyncio
+@pytest.mark.parametrize(
+    "item", [{"root": "config", "path": "layercove/job/cube.gcode"}, {"root": "gcodes", "path": "cube.gcode"}]
+)
+async def test_upload_rejects_server_that_loses_dispatch_namespace(item):
+    from backend.app.services.moonraker_http import MoonrakerHTTPClient, MoonrakerHTTPError
+
+    async def resolver(*_):
+        return {ipaddress.ip_address("192.168.1.25")}
+
+    client = MoonrakerHTTPClient(
+        base_url="http://printer.lan:7125",
+        resolver=resolver,
+        transport_factory=lambda *_: httpx.MockTransport(lambda _: httpx.Response(201, json={"item": item})),
+    )
+    with pytest.raises(MoonrakerHTTPError, match="did not preserve"):
+        await client.upload_gcode(io.BytesIO(b"G28"), filename="cube.gcode", directory="layercove/job")
diff --git a/backend/tests/unit/test_moonraker_upload_filename.py b/backend/tests/unit/test_moonraker_upload_filename.py
index 2066024..6ef035f 100644
--- a/backend/tests/unit/test_moonraker_upload_filename.py
+++ b/backend/tests/unit/test_moonraker_upload_filename.py
@@ -1,4 +1,2 @@
-"""Readable Moonraker names must remain safe and unique across dispatch attempts."""
-
-from uuid import UUID
+"""The exported basename stays readable; the upload directory owns uniqueness."""
 
@@ -11,14 +9,16 @@ from backend.app.utils.filename import (
     validate_moonraker_gcode_basename,
+    validate_moonraker_upload_directory,
 )
 
-DISPATCH = "12345678-1234-4234-8234-123456789abc"
-
 
 @pytest.mark.parametrize(
-    "filename", ["Calibration Cube.gcode", "Calibration Cube.GCODE.3MF", "Calibration Cube.3mf.gcode.3mf"]
+    "filename",
+    [
+        "Calibration Cube_PLA_9m42s.gcode",
+        "Calibration Cube_PLA_9m42s.GCODE.3MF",
+        "Calibration Cube_PLA_9m42s.3mf.gcode.3mf",
+    ],
 )
-def test_model_name_and_selected_plate_precede_unique_suffix(filename):
-    assert derive_moonraker_upload_filename(filename, DISPATCH, 2) == (
-        f"Calibration_Cube-plate-2-{UUID(DISPATCH).hex}.gcode"
-    )
+def test_orca_basename_is_not_decorated_with_plate_or_dispatch_id(filename):
+    assert derive_moonraker_upload_filename(filename) == "Calibration Cube_PLA_9m42s.gcode"
 
@@ -40,4 +40,4 @@ def test_model_name_and_selected_plate_precede_unique_suffix(filename):
 def test_legacy_names_are_safe_basenames(filename, stem):
-    actual = derive_moonraker_upload_filename(filename, DISPATCH)
-    assert actual == f"{stem}-{UUID(DISPATCH).hex}.gcode"
+    actual = derive_moonraker_upload_filename(filename)
+    assert actual == f"{stem}.gcode"
     validate_moonraker_gcode_basename(actual)
@@ -46,7 +46,7 @@ def test_legacy_names_are_safe_basenames(filename, stem):
 @pytest.mark.parametrize("prefix", ["x", "å", "模型", "🧱"])
-def test_utf8_truncation_preserves_complete_suffix(prefix):
-    actual = derive_moonraker_upload_filename(prefix * 500 + ".gcode.3mf", DISPATCH, 1)
+def test_utf8_truncation_preserves_extension(prefix):
+    actual = derive_moonraker_upload_filename(prefix * 500 + ".gcode.3mf")
     assert len(actual.encode("utf-8")) <= MAX_FILENAME_BYTES
     assert actual.startswith(prefix)
-    assert actual.endswith(f"-plate-1-{UUID(DISPATCH).hex}.gcode")
+    assert actual.endswith(".gcode")
     validate_moonraker_gcode_basename(actual)
@@ -54,19 +54,25 @@ def test_utf8_truncation_preserves_complete_suffix(prefix):
 
-def test_all_uuid_bits_are_retained_for_retries_and_old_jobs():
-    other = "12345678-1234-4234-8234-123456789abd"
-    first = derive_moonraker_upload_filename("Cube.gcode", DISPATCH, 1)
-    assert first != derive_moonraker_upload_filename("Cube.gcode", other, 1)
-    assert first != derive_moonraker_upload_filename("Cube.gcode", DISPATCH, 2)
-    assert first == derive_moonraker_upload_filename("Cube.gcode", DISPATCH, 1)
-
-
-@pytest.mark.parametrize("plate", [0, -1, True, "2", 1.5])
-def test_invalid_plate_ids_are_rejected(plate):
+@pytest.mark.parametrize(
+    "directory",
+    [
+        "",
+        "/root",
+        "../other",
+        "safe/../other",
+        "safe/./other",
+        "safe//other",
+        "safe/",
+        r"safe\other",
+        "safe/\x00",
+        "safe/\u202e",
+        "x" * 256,
+    ],
+)
+def test_upload_directory_cannot_escape_root_or_be_normalized(directory):
     with pytest.raises(InvalidFilenameError):
-        derive_moonraker_upload_filename("Cube.gcode", DISPATCH, plate)
+        validate_moonraker_upload_directory(directory)
 
 
-def test_dispatch_id_cannot_be_a_path():
-    with pytest.raises(ValueError):
-        derive_moonraker_upload_filename("Cube.gcode", "../another-job")
+def test_safe_dispatch_directory():
+    validate_moonraker_upload_directory("layercove/12345678123442348234123456789abc")
 
@@ -75,2 +81,2 @@ def test_source_must_be_a_real_string():
     with pytest.raises(TypeError):
-        derive_moonraker_upload_filename(None, DISPATCH)
+        derive_moonraker_upload_filename(None)
diff --git a/backend/tests/unit/test_scheduler_provider_dispatch.py b/backend/tests/unit/test_scheduler_provider_dispatch.py
index 4065f63..dea737a 100644
--- a/backend/tests/unit/test_scheduler_provider_dispatch.py
+++ b/backend/tests/unit/test_scheduler_provider_dispatch.py
@@ -108,5 +108,6 @@ async def test_moonraker_upload_claim_start_has_no_bambu_options(moonraker_queue
     assert upload_job.file.closed
-    assert upload_job.filename.startswith("queued-")
-    assert upload_job.filename.endswith(".gcode")
-    assert "cube" not in upload_job.filename
+    assert upload_job.filename == "cube.gcode"
+    assert item.provider_correlation_id
+    assert upload_job.directory == f"layercove/{item.provider_correlation_id.replace('-', '')}"
+    assert archive.filename == source.name
     assert upload_job.size == source.stat().st_size
diff --git a/backend/tests/unit/test_slicer_filename.py b/backend/tests/unit/test_slicer_filename.py
new file mode 100644
index 0000000..7a915d5
--- /dev/null
+++ b/backend/tests/unit/test_slicer_filename.py
@@ -0,0 +1,69 @@
+"""Regression coverage for Orca's default model/material/duration convention."""
+
+import pytest
+
+from backend.app.utils.slicer_filename import default_klipper_filename
+
+
+@pytest.mark.parametrize(
+    "seconds,label",
+    [
+        (42, "42s"),
+        (582, "9m42s"),
+        (3599, "59m59s"),
+        (3629, "1h0m"),
+        (3630, "1h1m"),
+        (86399, "1d0h0m"),
+        (90030, "1d1h1m"),
+    ],
+)
+def test_orca_short_time_matches_pinned_engine(seconds, label):
+    assert (
+        default_klipper_filename("3dbenchy.stl", b"; filament_type = PLA\nG28\n", seconds)
+        == f"3dbenchy_PLA_{label}.gcode"
+    )
+
+
+@pytest.mark.parametrize(
+    "filename", ["Cube.stl", "Cube.3mf", "Cube.gcode.3mf", "Cube.gcode.3mf.gcode.3mf", r"C:\models\Cube.stl"]
+)
+def test_source_suffixes_do_not_accumulate(filename):
+    assert default_klipper_filename(filename, b"; filament_type = PLA\n", 582) == "Cube_PLA_9m42s.gcode"
+
+
+@pytest.mark.parametrize("tool", [b"; initial_tool = 1\n", b"; initial_extruder = 1\n", b"T1\n"])
+def test_initial_tool_selects_material_not_first_or_source_archive(tool):
+    assert default_klipper_filename("Cube.stl", b"; filament_type = PLA;PETG\n" + tool, 582) == "Cube_PETG_9m42s.gcode"
+
+
+def test_json_types_and_gcode_duration_fallback():
+    content = (
+        b'; filament_type = ["PLA", "ABS"]\n; initial_tool = 1\n; estimated printing time (normal mode) = 2h 59m 40s\n'
+    )
+    assert default_klipper_filename("Cube.stl", content, 0) == "Cube_ABS_3h0m.gcode"
+
+
+@pytest.mark.parametrize(
+    "content,seconds",
+    [
+        (b"G28\n", 582),
+        (b"; filament_type = PLA\n", 0),
+        (b"; filament_type = PLA;ABS\n", 582),
+        (b"; filament_type = PLA\n; initial_tool = 999\n", 582),
+        (b"; filament_type = [\n", 582),
+    ],
+)
+def test_incomplete_metadata_retains_name_without_invented_labels(content, seconds, caplog):
+    assert default_klipper_filename("Cube.stl", content, seconds) == "Cube.gcode"
+    assert "lacks material/tool or duration metadata" in caplog.text
+
+
+def test_config_at_end_of_large_artifact_and_unicode_names():
+    content = b"G28\n" + b"; filler\n" * 600_000 + b"; filament_type = PLA\n"
+    name = default_klipper_filename("模型" * 200 + ".stl", content, 582)
+    assert len(name.encode("utf-8")) <= 255
+    assert name.startswith("模型") and name.endswith("_PLA_9m42s.gcode")
+
+
+def test_safe_spaces_are_preserved_and_unsafe_generated_characters_sanitized():
+    assert default_klipper_filename("My Cube.stl", b"; filament_type = PETG/CF\n", 582) == "My Cube_PETG_CF_9m42s.gcode"
